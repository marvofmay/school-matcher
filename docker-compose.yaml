services:
  service-operon:
    container_name: service-operon
    build:
      context: ./
      dockerfile: Dockerfile
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "81:80"
    volumes:
        - ./:/var/www/html
